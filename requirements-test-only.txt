# Minimal dependencies to run tests for generated_text_based metrics only
# (The generated_text_based metrics don't use torch)
numpy>=2.1.3
pytest>=8.3.3

# Optional: for coverage reports
coverage>=7.6.4
